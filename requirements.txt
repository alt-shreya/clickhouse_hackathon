# All three agents (Context/Instrumentation/Analytics) share one LLM provider,
# resolved by agents/config.py:get_config(). Anthropic is preferred when
# ANTHROPIC_API_KEY is set (agents/config.py:AnthropicConfig, adapted to the
# same OpenAI-shaped .chat.completions.create() interface every agent call
# site uses); OpenRouter (OPENROUTER_API_KEY, via the `openai` package below)
# is the fallback -- kept for when Anthropic isn't configured, or its shared
# free-tier model is preferred for cost reasons despite being less reliable.

# Langfuse -- required by the problem statement for tracing every LLM call and SQL span
langfuse>=2.53.0

# python-dotenv -- convenience for loading CLICKHOUSE_*/ANTHROPIC_*/LANGFUSE_* env vars
# from a local .env instead of hardcoding credentials in scripts


# Core
clickhouse-connect>=1.6.0

# ClickHouse MCP -- AnalyticsAgent talks to ClickHouse through the official
# ClickHouse MCP server (agents/analytics/mcp_client.py) when it's installed,
# falling back to clickhouse-connect directly otherwise.
mcp>=1.2.0
mcp-clickhouse>=0.1.8

# LLM (OpenRouter - OpenAI compatible; fallback provider)
openai>=1.0.0

# LLM (Anthropic direct; preferred provider when ANTHROPIC_API_KEY is set)
anthropic>=0.40.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.1


# Development
pytest>=7.0.0
black>=23.0.0
ruff>=0.1.0
