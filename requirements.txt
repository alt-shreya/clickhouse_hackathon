# All three agents (Context/Instrumentation/Analytics) share one LLM provider:
# OpenRouter, via the `openai` package below (OpenAI-compatible endpoint). See
# agents/config.py:make_llm_call_fn(). The `anthropic` package is not used --
# ANTHROPIC_API_KEY in .env is currently inert; set OPENROUTER_API_KEY instead.

# Langfuse -- required by the problem statement for tracing every LLM call and SQL span
langfuse>=2.53.0

# python-dotenv -- convenience for loading CLICKHOUSE_*/ANTHROPIC_*/LANGFUSE_* env vars
# from a local .env instead of hardcoding credentials in scripts


# Core
clickhouse-connect>=1.6.0

# LLM (OpenRouter - OpenAI compatible)
openai>=1.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.1


# Development
pytest>=7.0.0
black>=23.0.0
ruff>=0.1.0
